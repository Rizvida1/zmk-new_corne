#
# Copyright (c) 2024 The ZMK Contributors
# SPDX-License-Identifier: MIT
#
# go to sleep after one hour (1*60*60*1000ms)

CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=3600000

CONFIG_ZMK_SLEEP=y





CONFIG_WS2812_STRIP=y
CONFIG_ZMK_RGB_UNDERGLOW=y
CONFIG_ZMK_RGB_UNDERGLOW_ON_START=n

CONFIG_ZMK_RGB_UNDERGLOW_AUTO_OFF_IDLE=y
CONFIG_ZMK_RGB_UNDERGLOW_HUE_START=160
CONFIG_ZMK_RGB_UNDERGLOW_EFF_START=3

# ── zmk-nice-oled Display Configuration ──────────────────────────
CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y
CONFIG_NICE_EPAPER_ON=y

# ── Performance (ePaper defaults from OPTIMIZE.md) ───────────────
CONFIG_LV_Z_MEM_POOL_SIZE=8192
CONFIG_LV_Z_VDB_SIZE=100
CONFIG_ZMK_DISPLAY_DEDICATED_THREAD_STACK_SIZE=2560

# ── WPM + Bongo Cat ──────────────────────────────────────────────
# Widget container is 160×68 landscape (CANVAS_HEIGHT × CANVAS_WIDTH).
# Bongo cat is an LVGL object using direct landscape coordinates:
#   CUSTOM_X: 0-159 (horizontal), CUSTOM_Y: 0-67 (vertical, 0=top).
# ePaper module defaults (X=100, Y=8) place the cat center-right near top.
CONFIG_NICE_OLED_WIDGET_WPM=y
CONFIG_NICE_OLED_WIDGET_WPM_NUMBER=n
CONFIG_NICE_OLED_WIDGET_WPM_SPEEDOMETER=n
CONFIG_NICE_OLED_WIDGET_WPM_GRAPH=n
CONFIG_NICE_OLED_WIDGET_WPM_LUNA=n
CONFIG_NICE_OLED_WIDGET_WPM_BONGO_CAT=y
CONFIG_NICE_OLED_WIDGET_RESPONSIVE=n
CONFIG_NICE_OLED_WIDGET_BONGO_CAT_CUSTOM_X=100
CONFIG_NICE_OLED_WIDGET_BONGO_CAT_CUSTOM_Y=8

# ── Modifiers: 1×4 horizontal row, Windows symbols ─────────────
# VER layout increments in portrait canvas-Y; after 90° rotation
# canvas-Y → landscape-X, so VER = horizontal row in landscape.
# VER_ALIGN_CENTER auto-centers vertically (hardcoded base_x=27 → landscape Y=27).
CONFIG_NICE_OLED_WIDGET_MODIFIERS_INDICATORS=y
CONFIG_NICE_OLED_WIDGET_MODIFIERS_INDICATORS_FIXED=y
CONFIG_NICE_OLED_WIDGET_MODIFIERS_INDICATORS_FIXED_BOX=n
CONFIG_NICE_OLED_WIDGET_MODIFIERS_INDICATORS_FIXED_HOR=n
CONFIG_NICE_OLED_WIDGET_MODIFIERS_INDICATORS_FIXED_VER=y
CONFIG_NICE_OLED_WIDGET_MODIFIERS_INDICATORS_FIXED_SYMBOL=y
CONFIG_NICE_OLED_WIDGET_MODIFIERS_INDICATORS_FIXED_SYMBOL_MACOS=n
CONFIG_NICE_OLED_WIDGET_MODIFIERS_INDICATORS_FIXED_SYMBOL_WINDOWS=y
CONFIG_NICE_OLED_WIDGET_MODIFIERS_INDICATORS_LUNA=n
CONFIG_NICE_OLED_WIDGET_MODIFIERS_INDICATORS_BONGO_CAT=n

# ── HID indicators off — default X=100 is off-screen; only fires on ─
# CapsLock anyway, not worth the complexity right now
CONFIG_NICE_OLED_WIDGET_HID_INDICATORS=n

# ── Battery: both halves at module default (X=26, Y=19) ──────────
# Right half shows 0% without the BLE battery proxy — enable it.
# PERIPHERAL_ALL instead of PERIPHERAL_AND_CENTRAL: functionally
# identical for a 2-device split (shows central + peripheral battery),
# but suppresses the hardcoded "SIG" label in output.c which only
# appears when PERIPHERAL_ALL is NOT set.
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=y
CONFIG_NICE_OLED_WIDGET_CENTRAL_SHOW_BATTERY_PERIPHERAL_ALL=y

# Landscape layout (160px wide × 68px tall after 90° canvas rotation):
#   All canvas text coords rotate: landscape_X = 159-portrait_Y,
#                                  landscape_Y = portrait_X
#   Bongo cat / LVGL objects use landscape coords directly.
#
#   landscape X=13   Layer name (portrait Y=146, X=0)
#   landscape X=30   BT profile symbol (portrait Y=129, X=18)
#   landscape X=49-97 Modifier row ⊞ Alt ⇧ ⌃ (VER, Y=27 auto)
#   landscape X=100+ Bongo Cat (LVGL obj, X=100, Y=8)
#   landscape X=140  Battery "L%  R%" (portrait Y=19, X=0)

# Peripheral (right half): hammerbeam-slideshow (nice_view_custom shield)
# 30 images × 30s each = 900s total
CONFIG_CUSTOM_ANIMATION_SPEED=900000

# Uncomment the following line to enable NKRO
CONFIG_ZMK_HID_REPORT_TYPE_NKRO=y

# Some operating systems have problems with full support for consumer keycodes.
# Uncomment the following line if keycodes labeled "consumer" like C_AC_SEARCH don't work
#CONFIG_ZMK_HID_CONSUMER_REPORT_USAGES_BASIC=y

#EC11 enable
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# Mouse enable
CONFIG_ZMK_POINTING=y

CONFIG_ZMK_BACKLIGHT=y
CONFIG_ZMK_BACKLIGHT_BRT_START=100
CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=y
# Uncomment the following line to increase the keyboard's wireless range
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=8
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=8

#soft off
CONFIG_ZMK_PM_SOFT_OFF=y